@using Syncfusion.Blazor.Popups
@using Syncfusion.Blazor.Inputs

@if (TskModel != null) {
    <SfDialog Height="75%" Width="435px" Target="#target" ShowCloseIcon="true" @bind-Visible="@IsDialogShowing" >
        <DialogTemplates>
            <Header>
                @*<span class="e-avatar template-image e-avatar-xsmall e-avatar-circle"></span>
                <div id="template" title="Nancy" class="e-icon-settings">Nancy</div>*@
                <h2>@TskModel?.Name Bizzle</h2>
            </Header>
            <Content>
                <div class="dialogContent">
                
                    @*<div class="form-group">
                         <label for="first-name">ID:</label>
                         @TskModel?.Id
                     </div>

                    <div class="form-group">
                         <label for="first-name">Name:</label>
                         <SfTextBox ID="@TskModel?.Name"></SfTextBox>
                     </div>

                     <div class="form-group">
                         <label for="first-name">External ID:</label>
                         <SfTextBox ID="@TskModel?.ExternalId"></SfTextBox>
                     </div>*@
                

                    <EditForm Model="@TskModel" OnValidSubmit="@Save">
                       <DataAnnotationsValidator />
                       <ValidationSummary />
                        <label for="name">Name: </label>
                        <InputText Id="name" @bind-Value="@TskModel.Name"></InputText>

                        <label for="externalId">External ID: </label>
                        <InputText Id="externalId" @bind-Value="@TskModel.ExternalId"></InputText>

                        @*<label for="age">Age: </label><br />
                        <InputNumber Id="age" @bind-Value="@TskModel.Age"></InputNumber>*@

                        <button type="submit">SAVE</button>
                    </EditForm>

                   @*
                      <EditForm Model="@TskModel">
                         <DataAnnotationsValidator />
                         <ValidationSummary />
                         <div class="form-group">
                             <label for="first-name">Name:</label>
                             <SfTextBox ID="first-name"></SfTextBox>
                         </div>
                         <SfButton>Submit</SfButton>
                     </EditForm>*@
                </div>
            </Content>
            <FooterTemplate>
                @*<SfTextBox ID="inVal" @bind-Value="@TextBoxValue" @ref="TextboxObj" Type="InputType.Text" Placeholder="Enter your message here!" />*@
                <SfButton @onclick="@Save">SAVE</SfButton>
                @*<button id="sendButton" class="e-control e-btn e-primary" data-ripple="true">Send</button>*@
            </FooterTemplate>
        </DialogTemplates>
        <DialogEvents Closed="@DialogCloseHandler"></DialogEvents>
    </SfDialog>
}
